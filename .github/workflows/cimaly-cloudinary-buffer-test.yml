name: Cimaly Cloudinary Buffer Test

on:
  workflow_dispatch:
    inputs:
      publish_now:
        description: "Publish now? true / false"
        required: true
        default: "false"

jobs:
  publish:
    runs-on: ubuntu-latest

    env:
      BUFFER_ACCESS_TOKEN: ${{ secrets.BUFFER_ACCESS_TOKEN }}
      BUFFER_FACEBOOK_PROFILE_ID: ${{ secrets.BUFFER_FACEBOOK_PROFILE_ID }}
      BUFFER_INSTAGRAM_PROFILE_ID: ${{ secrets.BUFFER_INSTAGRAM_PROFILE_ID }}
      PUBLISH_NOW: ${{ github.event.inputs.publish_now }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Publish Cloudinary images to Buffer
        run: node scripts/publish-buffer-from-cloudinary.js
