name: Cimaly Daily Social (ChatGPT images only)

on:
  workflow_dispatch:

jobs:
  chatgpt-images-only:
    runs-on: ubuntu-latest
    steps:
      - name: ChatGPT image policy
        run: |
          echo "Cimaly automatic Sharp/TMDB image generation is disabled."
          echo "Use .github/workflows/import-cimaly-images.yml with ChatGPT-generated image URLs."
          echo "Higgsfield is not used."
